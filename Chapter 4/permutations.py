

initial = input("Please enter three numbers in the form x1, x2, x3:\t").split(", ")
numbers = list(initial)

def factorial(num):
	value = 1
	for i in range(1, num+1):
		value *= i
	return(value)

def permutations(iter):
	n = len(iter)
	k = 0
	while  = ["" for i in range(factorial(n))]

	for i in range(len(result)):
		for j in range(i, n):
			for x in range(j+1, n):
				for z in range(x+1, n):
					result[i] = result[i] + iter[j] +  iter[x] +  iter[z]  		

